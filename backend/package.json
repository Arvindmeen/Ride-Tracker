{
  "name": "ridetracker-backend",
  "version": "1.0.0",
  "description": "RideTracker backend — Node.js + Express + PostgreSQL + Kafka + Redis",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "express-rate-limit": "^7.1.5",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "uuid": "^9.0.0",
    "zod": "^3.22.4",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "comments": {
    "pg": "PostgreSQL client — install when DB is ready: npm i pg",
    "ioredis": "Redis client — install when cache is ready: npm i ioredis",
    "kafkajs": "Kafka client — install when messaging is ready: npm i kafkajs",
    "ws": "WebSocket server — install when real-time is ready: npm i ws",
    "@elastic/elasticsearch": "Elasticsearch — install when search is ready"
  }
}
